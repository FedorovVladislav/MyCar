//
//  MapData.swift
//  MyCar
//
//  Created by Елизавета Федорова on 23.10.2021.
//

import Foundation
import MapKit

class MapdflUIViewController: UIViewController {
    /*
    var text: String = ""
    var alertTextField: UITextField?
    
    
    @IBOutlet weak var MKMapView: MKMapView!
    
    @IBAction func AddAdress(_ sender: UIButton) {
    }
    
    @IBAction func AddPointOnMap(_ sender: UIButton) {
        Alert()
    }
    func Alert(){
        let alertView = UIAlertController(title: "Add Point", message: "Write adress location", preferredStyle: UIAlertController.Style.alert)
        
        alertView.addTextField(configurationHandler: { textField -> Void in
            self.alertTextField = textField
        })
        alertView.addAction(UIAlertAction(title: "Cancel", style: UIAlertAction.Style.cancel, handler: nil))
        alertView.addAction(UIAlertAction(title: "Add", style: UIAlertAction.Style.default, handler: { ACTION in
            if let text = self.alertTextField?.text
            {
            print(text)
            }
        }))
        self.present(alertView, animated: true, completion: nil)
        
    }
     */
     
}
